import { Home } from "../../containers/Home/Home";

type LandingProps = {
    // name: string;
};

export const Landing = (props: LandingProps) => {
    return (
        <div>
            <Home />
        </div>
    );
};
